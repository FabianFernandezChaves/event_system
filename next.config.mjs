/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com'],  // Permitir cargar imágenes desde este dominio
  },
};

export default nextConfig;
